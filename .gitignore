node_modules
.next
out
build
dist
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env*.local

# Runtime data (users.json, setup.json) — file-backed defaults; US-002 replaces with Postgres
data/

# Scenario sample-data fixtures (local/dev, regenerated by sim scripts) — mirrors
# the prior seed/<name>/data/ ignore now that data lives under scenarios/<name>/seed/.
# The .generated.sql bundle stays tracked; the raw JSON rows do not.
scenarios/*/seed/*.json

# Test scratch dirs for function-backed action runner (US-025)
lib/actions/__test_fixtures__/
functions/__sandbox_fixtures__/

# Vitest scratch dir for fixtures that must live inside the project root per
# [[gotcha-vitest-vite-fixture-root]] (runtime-persist.test.ts)
.tmp/

.DS_Store

# transient book-club second-instance (docker demo) — not committed
book-club-instance/
# transient empty second-instance (docker demo, :3032) — not committed (scaffolding)
empty-instance/
.decontam-baseline.txt

# Ephemeral session/loop coordination + stray backups & scratch — never committed
.chora/
.opponent-log.md
.m3-backup/
ontology-backup-small-community/
/package-lock.json
*casing-test.mjs
