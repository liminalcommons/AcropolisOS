
### 2026-05-26 ~ — Via Negativa (cycle 10, audit of the governance round-trip test + check_out)
Per cycle-9-Negativa's own standard, I did NOT drive the flaky chat. I audited the deterministic deliverables directly.

**Round-trip test (`check-in-confirm-roundtrip.test.ts`) — VERIFIED GENUINE, NOT a tautology.** Read all 209 lines. It wires the REAL machinery: `loadOntology(ONTOLOGY_ROOT)`, `buildObjectPermissionsMap`, `createCtx` (the permission wrapper), `createInProcessDispatcher` pointed at the REAL `functions/` dir, `runApplyActionTool` + `resolveActionPolicy`. Only the *store* is `createInMemoryStore` — the legitimate DB double; the permission wrapper (`wrapObjectAccess`) is store-agnostic (Step 1), so the gate behaves identically to pg-store. **Step A** (no bypass): asserts `ok:false` + `confirmation_required` envelope (action/params/`reason:"always_confirm"`/`required_permissions:[steward,manager]`) + `result.result` undefined AND re-reads Booking (still `confirmed`) + Guest (still `booked`) → proves the handler genuinely did NOT run. **Step B** (`bypassConfirmation:true`, exactly what `confirm/route.ts` sets server-side): asserts `ok:true`, no confirmation envelope, the handler's checked-in envelope, Booking/Guest read back `checked_in`, and an `ok` audit row linked by `audit_id`. This is a real agent→confirm→handler→mutation proof. Strong.

**check_out handler — VERIFIED CORRECT MIRROR (read functions/check-out.ts).** Not a copy-paste error: Booking→`completed` (correct, NOT `checked_in`), Guest.current_status→`checked_out`. Same `findById` existence guard, same `update_failed` guard, same guest-coherence (graceful if guest missing/unreadable), same fail-closed permission inheritance (dispatcher gates [steward,manager] before handler; ctx.objects re-enforces). agent_policy unchanged.

**Re-ran independently: 8/8 pass** (`check-in-confirm-roundtrip` + `check-out` test). tsc clean (prior).

**Findings:** HIGH/MED security or correctness: NONE this cycle. The round-trip test is a faithful, store-double-only integration proof; the check_out mirror is correct and double-fail-closed.
- **One genuine finding (PRE-EXISTING, not this cycle's regression):** `dismiss_blocker` declares `function: dismiss-blocker` in `ontology/action-types/dismiss-blocker.yaml` but `functions/dismiss-blocker.ts` DOES NOT EXIST → the action is un-executable (fails at function resolution; this is why `resolve-blocker-with-pathway.test.ts` fails). Same class as check_in/check_out before cycle 8. It is a real platform hole — a declared platform action that cannot run — but it predates this loop.

**Ontological coherence verdict: GOOD.** No new abstraction. The round-trip test reuses the same harness as `log-incident-auto-apply.test.ts`; check_out reuses the function-backed `defineAction` pattern. Verification has fully pivoted off the flaky chat onto deterministic integration tests — the right call.

**Quality trend: EXCELLENT.** The governance contract is now LOCKED deterministically on both arms (auto_apply commits; always_confirm gates → confirm applies), with the round-trip test as the durable standard. The remaining frontier is genuinely NEW, not polish: (1) **fix `functions/dismiss-blocker.ts`** (un-executable platform action — highest-value next build, closes the last function-backed hole) + `functions/claim-shift.ts`; (2) the **veto-queue VIEW** — always_confirm actions currently return `confirmation_required` INLINE one-at-a-time in chat; the user's testbed vision ("28 auto-applied, M awaiting you") needs a PERSISTENT queue surface the steward can watch — this is the real unbuilt half of the AI-governance testbed; (3) Step 3 /day-as-generated. Recommend Positiva take (1) then (2).

### 2026-05-26 ~ — Via Positiva (cycle 11, build) — dismiss_blocker handler: LAST un-executable function-backed action closed (75742aad0)
Honored cycle-10-Negativa's #1 fix-request before anything new.
- **`functions/dismiss-blocker.ts`** — the missing handler that made `dismiss_blocker` un-executable (failed at function resolution) and broke `resolve-blocker-with-pathway.test.ts`. A faithful mirror of `resolve-blocker-with-pathway.ts`: `findById` guard → `not_found`; `status !== "open"` → `not_open` (status echoed); `ctx.objects.AgentBlocker.update(status:"dismissed")` (ONLY status — AgentBlocker has no `dismissed_at` column, enum is [open,resolved,dismissed,expired]); self-notify (kind `agent_unblocked`, carries `reason` if supplied) so the agent principal can re-prompt. Returns `{ ok, blocker_id }`.
- **Permission is upstream, not in the handler** (correct, matches the mirror): `member_self` ownership is enforced by `enforceActionPermission` BEFORE the handler — cross-member dismiss + non-existent-blocker both `reject(/cannot invoke action/)`; steward bypasses ownership. The handler is intentionally role-agnostic. agent_policy unchanged (always_confirm).
- **VERIFY: 9/9 pass** (re-ran independently: 5 existing resolve + 4 new dismiss — own/cross-deny/not-undoable/steward). **tsc clean** (no dismiss-blocker errors). `git status` confirms ONLY `functions/dismiss-blocker.ts` added — no stray files.
- **No Chrome step: this is a backend governance handler, not a route/component.** Per the cycle-9/10 governance-verification standard (chat UI chronically flaky), the deterministic test IS ground truth here. No browser claim made.

**Governance state now COMPLETE on the action layer:** every function-backed action has a handler — check_in ✓, check_out ✓, dismiss_blocker ✓ (this cycle). `claim_shift` is the only remaining one to double-check (targets Shift — confirm assignee field + member-less-steward semantics; may already work via the dispatcher). auto_apply (log_incident) + always_confirm (check_in/out, dismiss_blocker) both proven.

**Next frontier (the real unbuilt half — recommend Positiva take this next):** the **veto-queue VIEW**. Right now always_confirm actions surface `confirmation_required` INLINE, one-at-a-time, in chat. The user's testbed vision ("AI applies a batch → 28 auto-applied, M awaiting you") needs a PERSISTENT queue surface the steward can watch — a place where pending always_confirm actions accumulate with Confirm/Reject. Also: AgentBlocker rows (open status) are exactly this kind of "awaiting human" queue and already have read perms — a `/me`-style or `/org` veto/blocker queue widget (governed catalog: a new "queue" or reuse roster/data_table over AgentBlocker filtered status=open) would surface BOTH agent-blockers AND pending confirmations. THIS is the keystone of the AI-governance testbed and is genuinely unbuilt. Via Negativa: audit dismiss_blocker (mirror correctness + that the self-notify can't leak cross-member) — it's deterministic, no chat needed.

### 2026-05-26 ~ — Via Negativa (cycle 12, audit of dismiss_blocker handler)
Independent verification (NOT trusting the log): re-ran `functions/resolve-blocker-with-pathway.test.ts` → **9/9 pass** (5 resolve + 4 dismiss). `tsc --noEmit` → **0 `error TS`** (genuinely clean, confirmed by `grep -c`). No Chrome step: dismiss_blocker is a backend handler with no route/component, the veto-queue view isn't built yet, so there is no UI surface to screenshot — deterministic tests are ground truth here (consistent with the cycle-9/10 standard). Honest: no browser claim.

**HIGH:** none.

**MED — WRONG NOTIFICATION RECIPIENT in the steward-override path (real, untested, defeats documented intent).**
`functions/dismiss-blocker.ts:` notifies `recipient_member_id: ctx.actor.userId`. But the action's own doc says "agent principal is notified so it can re-prompt," and the principal is the BLOCKED member (`row.blocked_actor_id`), not the dismisser. Proof chain:
- `functions/flag-blocker.ts:70` writes the blocker notification to `recipient_member_id: params.blocked_actor_id` (the principal) — the correct counterpart.
- `app/inbox/page.tsx:40` filters `listForRecipient(actor, actor.userId)` — a notification is ONLY ever visible to its `recipient_member_id`.
- Therefore when a STEWARD dismisses member A's blocker (the explicit "steward can dismiss any member's blocker" test, line 252), the "Dismissed" notification lands in the STEWARD's inbox; member A — and the agent watching member A's flow — never learn the blocker was cleared, so the agent stalls or re-prompts blindly. Self-notify is only coincidentally correct when `actor === blocked_actor_id` (self-dismiss).
- Untested: the 4 dismiss tests assert `ok`/`status` only, never the notification recipient, so the bug is invisible to the suite.
- **PATTERN, not isolated:** identical line in `resolve-blocker-with-pathway.ts:39`, `resolve-blocker-with-input.ts:56`, `resolve-blocker-with-custom.ts:49` — all 4 blocker-clearing handlers notify `ctx.actor.userId`. Cycle 11 faithfully MIRRORED a latent pre-existing defect. The 3 resolve handlers predate this loop, but the bug is the same.

**FIX REQUEST for Via Positiva (TDD, consistent across all 4 — do this BEFORE building the veto-queue view, or the queue will let a steward silently fail to notify members):**
1. RED: add a test to `functions/resolve-blocker-with-pathway.test.ts` — steward dismisses member A's OPEN blocker → assert `notifications` contains a row with `recipient_member_id === memberA.userId` (NOT the steward's). It will fail today.
2. GREEN: in all four handlers change `recipient_member_id: ctx.actor.userId` → `recipient_member_id: row.blocked_actor_id`. Correct for self-clear (actor===blocked member) AND steward-override (notifies the principal). `row.blocked_actor_id` is already loaded via `findById` in each handler.
3. Re-run; 9/9 + the new test green; tsc clean.

**LOW (observation, not a defect):** the four blocker-clearing handlers share a near-identical self-notify block. After the recipient fix, consider a tiny shared `notifyPrincipal(ctx, row, kind, body)` helper to keep the recipient correct in ONE place (YAGNI until the 4th was added — now it's the 4th, so it's borderline justified). Defer unless it recurs.

**Ontological-coherence verdict: GOOD with a caveat.** dismiss_blocker correctly reuses the function-backed `defineAction` pattern + upstream `member_self` enforcement (cross-member deny + steward bypass verified by tests) + `ctx.objects`. No new abstraction, no free-form code, fits the adaptive-architecture vision. The caveat is the propagated recipient bug above — coherence of the PATTERN was preserved at the cost of copying its flaw.

**Quality trend: SOLID but watch the mirror reflex.** Cycle 11 closed a real hole (last un-executable function-backed action) cleanly and fast. The risk this cycle surfaced: "faithful mirror" propagated a latent defect verbatim. Net still positive — the action layer is complete — but the recipient fix should land before the veto-queue view is built on top of these handlers.

### 2026-05-26 (morning) — Via Positiva (cycle 13, build) — fixed VN-12 MED: blocker-cleared signal now reaches the principal (5d6505619)
Honored VN cycle-12's #1 fix-request before any new frontier work (and it's the safe call with the user awake and checking — pure defect remediation, no new direction).
- **TDD RED:** added `functions/resolve-blocker-with-pathway.test.ts` test — steward dismisses memberA's open blocker → assert the `agent_unblocked` signal lands in memberA's inbox, NOT the steward's. Ran it: FAILED for the right reason (`expected [] to have length 1` — memberA got nothing; the signal went to the steward).
- **GREEN:** changed `recipient_member_id: ctx.actor.userId` → `row.blocked_actor_id` in all 4 blocker-clearing handlers (dismiss-blocker, resolve-blocker-with-{pathway,input,custom}). `row` is the `findById`-loaded AgentBlocker in each; `blocked_actor_id` is a required ref so the guard `if (ctx.notifications && row.blocked_actor_id)` is sound. Correct for BOTH self-clear (actor===principal) and steward-override (notifies the principal). Updated the now-inaccurate "self-notify"/"notify the actor" comments.
- **DISCOVERY (refines the VN-12 finding — there are TWO notification sources, not one):** the first GREEN run still failed because the steward ALSO had a notification. Traced it: `lib/actions/side-effects.ts:184` — the `notify_member` SIDE-EFFECT (dispatcher pipeline, declared in the action's `side_effects: [audit, notify_member]`) writes a SEPARATE generic "[acropolisOS] dismiss_blocker completed" receipt to the resolved recipient (the ACTOR for self-service actions = the steward). That receipt to the actor is legitimate and intentional — distinct from the handler's domain `agent_unblocked` signal. So the fix is ONLY about the handler's explicit notification (the semantic signal); the side-effect receipt is correctly left alone. Refined the test to assert by KIND (`agent_unblocked` reaches memberA; `agent_unblocked` does NOT reach the steward), robust to the coexisting receipt.
- **VERIFY:** pathway file **10/10** (incl. new principal test); regression `resolve-blocker-with-input.test.ts` **10/10**; **tsc 0 errors**. No Chrome step: handler-only change, `/inbox` route logic untouched (only which `recipient_member_id` the handlers write) — deterministic tests are ground truth.
- **Honesty note:** `resolve-blocker-with-custom.ts` has NO dedicated unit test in the tree, so that 4th edit is tsc-verified + pattern-identical only (mechanically the same one-line change as the 3 tested handlers).

**Coherence:** the LOW "shared notifyPrincipal helper" idea from VN-12 is now MORE justified (4 handlers, identical block, identical recipient rule) — but still YAGNI-deferred; the inline fix is correct and the duplication is stable. Flag for a future cleanup cycle, not now.
**Next frontier (unchanged, awaiting the user's call this morning):** the **veto-queue VIEW** — pending always_confirm actions + open AgentBlocker rows surfaced as a governed steward queue (Confirm/Reject). The governance action layer + its notification semantics are now both correct, so the queue can be built on a sound base. Via Negativa: audit this fix — confirm `row.blocked_actor_id` is always populated (required ref), the kind-filtered test genuinely distinguishes signal from receipt, and the custom handler (untested) didn't regress.

### 2026-05-26 (morning) — Via Positiva (cycle 14, build) — VETO-QUEUE v1: agent_blocker is a governed catalog type; open escalations render on /org (7cdce86ba)
User chose "B" (build the veto-queue) while awake. Built the FIRST coherent slice: the steward's "awaiting your decision" surface, by GOVERNED COMPOSITION — no new widget kind, no hand-coded blocker screen, no new SQL.
- **agent_blocker added to the governed vocabulary** (`lib/widgets/catalog.ts`): `CATALOG_VALID_TYPES` + `CATALOG_VALID_FIELDS` (summary/reason_kind/blocked_actor_id/status/resolution_mode/created_at/blocked_work_ref/detail). `lib/widgets/read-api.ts`: `agent_blocker → "AgentBlocker"` in `CATALOG_TYPE_TO_OBJECT_TYPE` + `TABLE_MAP` (the latter a hidden 2nd registration point — `Record<CatalogType,…>`, tsc-enforced).
- **data_table now carries an optional equality `filter`** (`{field,value}`, mirrors MetricConfig). The read-api `select` ALREADY parameterized + whitelist-validated a filter (read-api.ts:249-269) — only the config schema didn't expose it. Added `unknown_filter_field` validation in `validateWidgetConfig` (mirrors metric) so a bad field is rejected, not silently dropped.
- **/org default** (`lib/org-dashboard/store.ts`): renders `admin-veto-queue` (agent_blocker, filter status=open, limit 50) FIRST, then the existing bed-list.
- **VERIFY (Chrome = ground truth, tab 1025355378, steward session):** `/org` shows the AGENT_BLOCKER table as the first card with all 3 seeded open blockers — "Guest 2 days past expected departure" (decision/pathways/Pablo Rey), "Overbook bed D3-A2 for tonight walk-in?" (confirmation/confirm_binary/Lucía Aranzubia), "Charge EUR 40 no-show fee to booking BK-0007?" (risky_action/confirm_binary/Sara Mendoza) — ABOVE the bed inventory. `blocked_actor_id` is REF-RESOLVED to member names (the cycle-5 resolveRefLabels path works on the new type). tsc 0 errors; `vitest lib/widgets/ lib/org-dashboard/` 56/56.
- **Seed (verification data, NOT the seed pipeline):** inserted 3 open AgentBlocker rows via direct SQL (the hostel seed has no agent_blocker data; avoided the generated-SQL pipeline gotcha). NOTE: transient — a DB reset drops them. A future cycle could add `seed/hostel/data/agent-blocker.json` properly.

**Known gaps (logged, not defects):**
1. **Card header renders raw "AGENT_BLOCKER"** — `ResolvedWidgetCard`'s DataTableWidget derives the header from `config.type` with NO prettify and NO per-widget title. The steward's most important surface deserves "Awaiting your decision". FIX (next increment): add an optional `title` to the widget descriptor + render it (clean vocabulary growth — benefits every composed widget; also lets compose_view name widgets). Small, separable.
2. **No actions yet** — read-only. Confirm/Resolve/Dismiss buttons wired to the now-correct resolve_blocker_*/dismiss_blocker actions (through the confirm pipeline) is the NEXT big slice — that turns the queue from a list into the actual veto surface.
3. **member_self row-scoping vs the type-level catalog fence:** agent_blocker read perm is [steward, member_self]; the catalog `canReadType` is TYPE-level, so a steward sees ALL open blockers (correct for this steward-only view), but if agent_blocker is ever composed into a member-facing view, the catalog path does NOT row-filter to the member's own blockers. Out of scope now (/org is steward-only) — FLAG for Via Negativa to confirm + for whoever builds member-facing views.

**Next frontier:** (a) per-widget `title` (fixes gap 1, enables named composed widgets); (b) queue ACTIONS (gap 2 — the veto surface proper); (c) proper agent_blocker seed data. Via Negativa: independently verify the queue renders + is steward-fail-closed; probe gap 3 (does composing agent_blocker as a non-steward leak other members' blockers?); confirm the data_table filter is parameterized (no injection via field/value).

### 2026-05-26 (morning) — Via Negativa (cycle 15, audit of veto-queue v1)
Independent verification (reproduced, not trusted): Chrome tab 1025355378, steward session — `/org` renders the AGENT_BLOCKER card FIRST with all 3 open blockers, `blocked_actor_id` ref-resolved to member names (Pablo Rey / Lucía Aranzubia / Sara Mendoza), above the bed inventory. Re-ran `vitest lib/widgets/ lib/org-dashboard/` → 56/56; `tsc` → 0. Ground truth matches the cycle-14 claim.

**SECURITY — boundary VERIFIED SOUND (probed the builder's deferred "gap 3" directly):**
- The builder flagged "member_self row-scoping vs the type-level catalog fence" as out-of-scope. I traced it in CODE: `buildCanReadType` (read-api.ts:149) calls `actorMatchesTokens(actor, perms.read, null, …)` with **row=null**; `actorMatchesTokens` (ctx.ts:173) only matches `member_self` when `&& row` is truthy. So for agent_blocker `[steward, member_self]`, a plain `member` matches NEITHER token at the type level → `canReadType("agent_blocker")` = FALSE → select returns `[]`. **It fails CLOSED in the safe direction: a member is denied the type entirely, NOT leaked all rows.** Gap 3 is NOT a vulnerability — the builder undersold its own safety.
- `/org` is steward-only fail-closed (org/page.tsx:43 anon→block; :57 `role !== "steward"` → "available to stewards only").
- Filter is injection-safe: value is a bound parameter (`= ${filter.value}`); the field is `sql.raw`-interpolated ONLY after `allowed.has(filter.field)` against the per-type whitelist. Columns likewise (safeColumns + quoted whitelist).

**MED — FIXED THIS CYCLE (52c3c0bf1): select() fail-OPEN on a bad filter field.**
`read-api.ts select()` fell through to an UNFILTERED select when handed an unrecognized filter field → returned a silent SUPERSET (all rows). Its sibling `count()` (line 231) returns `0` for the identical `!allowed.has(filter.field)` condition. So a `metric` and a `data_table` given the same bad field DISAGREED — metric says 0, table shows everything. In a module whose every other invalid branch (unknown type, no perm, no valid cols) returns empty, this was the one fail-open; for a `status=open` veto-queue it meant a field typo would silently surface resolved/dismissed/expired blockers too. FIX: `if (!allowed.has(filter.field)) return { columns: [], rows: [] };` (pre-SQL, matches count). Flipped the builder's `read-api.test.ts` case that had codified the fall-through (it now asserts empty + 0 executeCalls). 56/56, tsc 0. NOT a security leak (perm gate is separate) — a correctness/consistency fail-open, now closed.

**LOW (already queued by builder, confirming it's real): raw "AGENT_BLOCKER" card header.** The steward's lead card literally reads "AGENT_BLOCKER" — `ResolvedWidgetCard`'s DataTableWidget derives the header from `config.type` with no prettify/title. Confirmed in Chrome. Builder logged it as gap 1; agreeing it should be the NEXT fix (a per-widget `title` on the descriptor).

**LOW (coherence smell): adding a catalog type now touches FOUR synchronized structures** — `CATALOG_VALID_TYPES`, `CATALOG_VALID_FIELDS` (catalog.ts), `TABLE_MAP`, `CATALOG_TYPE_TO_OBJECT_TYPE` (read-api.ts). tsc enforces the two `Record<CatalogType,…>` maps, but `CATALOG_VALID_FIELDS` omissions and a missing object-type-name entry fail only at runtime. Fragmentation risk as the vocabulary grows. Not urgent; flag for a future consolidation (derive fields/maps from one source, e.g. the ontology).

**ONE concrete improvement suggestion:** before adding queue ACTIONS (the next big slice), add the per-widget `title` (gap 1) — it's tiny, fixes the most visible defect, AND it is the natural place to render an action-affordance hint later. Sequence: title → actions, not actions → title.

**Ontological-coherence verdict: STRONG.** The veto-queue is pure governed composition — `agent_blocker` joined the catalog vocabulary, `data_table` gained a filter the read-api already supported, the surface is a default descriptor. No new widget kind, no hand-coded blocker screen, no free-form SQL, no duplicate abstraction. Fits the adaptive-architecture vision cleanly. The only coherence nit is the 4-registry fan-out (LOW above).

**Quality-trend: IMPROVING.** Cycle 14 shipped a real, vision-aligned feature verified in Chrome; this audit found the security story was actually SOUNDER than claimed (gap 3 is fail-closed) and closed the one genuine fail-open (select filter) by aligning it to count's existing precedent — a net hardening, not new surface. The dialectic is working: builder advances the frontier, auditor tightens the boundary. Next Positiva: title (gap 1) → queue actions.

### 2026-05-26 (morning) — Via Positiva (cycle 16, build) — per-widget TITLE: dashboard cards get human headers (b9b2ace2c)
Honored VN cycle-15's #1 suggestion (title BEFORE action buttons — it's tiny, fixes the most visible defect, and is where action affordances will live).
- **Optional `title?: string` threaded descriptor → resolve → render:** added to `WidgetDescriptor` (store.ts), `StoredDescriptor` + `ResolvedWidget` (compose.ts); `runDescriptors`/`resolveDashboard` thread `descriptor.title` onto each `ResolvedWidget`.
- **`ResolvedWidgetCard` header = `widget.title ?? prettify(config.type)`** in all 4 sub-renderers (metric/data_table/roster/calendar, both empty + populated branches, computed once per renderer). This ALSO fixes VN-15's LOW: untitled widgets now show "Agent Blocker", never raw "agent_blocker".
- **Defaults titled:** veto-queue → "Awaiting your decision", bed-list → "Bed inventory".
- **compose_view tool gains optional `title`** (route.ts inputSchema + composeOrgView input + instruction text) so the agent can NAME widgets it composes ("show me open shifts" → title "Open shifts"). `composedId` unchanged (title isn't identity).
- **VERIFY (Chrome = ground truth, tab 1025355378, steward, post-restart):** `/org` first card header now reads **"AWAITING YOUR DECISION"** (was "AGENT_BLOCKER"), second reads **"BED INVENTORY"** (was "BED"); the 3 open blockers + ref-resolved member names still render correctly beneath. tsc 0; `vitest lib/widgets/ lib/org-dashboard/` 56/56. No test needed updating (no strict toEqual on a full ResolvedWidget / DEFAULT_ORG_DASHBOARD).
- **Coherence:** pure additive presentation field; no new widget kind, no styling/token changes (label text only), no permission touch. The 4-registry fan-out VN-15 flagged is untouched (separate concern).

**Next frontier (the big one): queue ACTIONS.** Turn the read-only veto-queue into the actual veto surface — Confirm/Resolve/Dismiss affordances on each open-blocker row, wired to the now-correct `resolve_blocker_*`/`dismiss_blocker` actions through the confirm pipeline (`app/api/chat/confirm` sets bypassConfirmation server-side; steward-gated). This is a real design problem: the catalog widgets are READ-ONLY renderers, so actions need a governed mechanism (a new interactive widget kind? per-row action buttons driven by the ontology's action_types whose target = AgentBlocker? an action column?). Recommend Via Negativa pressure-test the DESIGN options next, and Via Positiva build the smallest action slice (e.g. a single "Dismiss" affordance on a blocker row → confirm → row leaves the open queue). Via Negativa this cycle: verify the title fallback can't render `undefined`/break on a titleless legacy descriptor, and that compose_view's title can't inject markup (it's React text — should be auto-escaped, but confirm no dangerouslySetInnerHTML in the card).

### 2026-05-26 (morning) — Via Negativa (cycle 17, audit of per-widget title)
Independent verification (reproduced fresh, Chrome tab 1025355378, steward): `/org` first card header = **"AWAITING YOUR DECISION"**, second = **"BED INVENTORY"** (the raw "AGENT_BLOCKER"/"BED" are gone); 3 open blockers + ref-resolved names render beneath. Re-ran `vitest lib/widgets/ lib/org-dashboard/` 56/56; tsc 0. Matches the cycle-16 claim.

**SECURITY — title is safe:**
- No `dangerouslySetInnerHTML` anywhere in ResolvedWidgetCard / org page / home page — `widget.title` renders as auto-escaped React text. No XSS via an agent-supplied title.
- No double-titling: `app/org/page.tsx:112-113` renders bare `<ResolvedWidgetCard>` in a map; the page's only heading is the `<h1>` "Org dashboard" (line 88). The card owns its header — one title path, not two.

**HIGH/MED: none.**

**LOW — FIXED THIS CYCLE (b59cfdba0): unbounded title.** `compose_view`'s `title` was `z.string().optional()` with no max — an agent-supplied (LLM-driven, hence semi-untrusted) title of arbitrary length could break the card header layout. Steward-only + self-inflicted (no security impact), but a missing input bound at a system boundary. Added `.max(120)` at the zod schema (the entry point); composeOrgView/WidgetDescriptor trust validated input, so one bound suffices. tsc 0, no runtime change for valid titles.

**LOW (coherence observation, NOT fixed — flag for when titling matures): a SECOND label path exists.** `app/page.tsx:129` uses `widgetLabel(w.kind, w.config)` for the home dashboard's widget MENU/nav chips, deriving a label from kind+config — independent of the new `widget.title ?? prettify(type)` in ResolvedWidgetCard. They serve different surfaces (menu chip vs card header), so it's not a conflict today (home widgets are titleless), BUT once widgets carry titles the menu chip won't reflect them — divergence. NOT the `ontologyToGraph`-style duplicate (different purpose, pre-existing), just a label-derivation that should eventually prefer `descriptor.title` too. Suggest Via Positiva fold `widgetLabel` to prefer title when it next touches that file.

**Regression check:** home dashboard (`/`) also consumes ResolvedWidgetCard, but the steward account has "No Member row" (pre-existing — steward isn't a seeded hostel Member), so the per-user home isn't a live surface here. Title is additive + falls back to `prettify`, so any titleless member descriptor renders strictly better than the pre-cycle-16 raw type. No regression.

**ONE concrete improvement suggestion:** before/with the queue-actions slice, fold `widgetLabel` (app/page.tsx) to prefer `descriptor.title` so the home menu and the card agree — closes the LOW divergence above in the same area you'll be editing.

**Ontological-coherence verdict: STRONG.** Title is a pure additive presentation field threaded through the existing descriptor→resolve→render pipeline. No new widget kind, no free-form code, no styling/token drift (label text only), no permission change. Fits the adaptive-architecture vision. The only nit is the pre-existing second label path (LOW above).

**Quality-trend: IMPROVING / STEADY-EXCELLENT.** Cycle 16 fixed the single most visible defect cleanly and verified it in Chrome; this audit found NO security/correctness issues — only an unbounded-input LOW (now closed) and a latent label-path divergence (flagged). The boundary is intact and slightly tighter than before. Next Positiva: queue ACTIONS — and pressure-test the DESIGN first (catalog widgets are read-only renderers; per-row Confirm/Resolve/Dismiss needs a governed mechanism — likely a new interactive widget kind or an action-column driven by action_types whose target=AgentBlocker, NOT hand-coded buttons). The smallest honest slice: one "Dismiss" affordance on a blocker row → confirm pipeline → row leaves the open queue.

### 2026-05-26 (morning) — Via Positiva (cycle 18, build) — QUEUE ACTIONS: governed Dismiss works end-to-end + fixed a CRITICAL latent infra bug (da2807d9e fix, beb65addd feat)
The actions frontier. Designed it governed-first (per VN-17): catalog widgets are read-only renderers, so per-row actions are DERIVED from the ontology, not hardcoded.

**FEATURE (beb65addd): governed per-row Dismiss on the veto-queue.**
- `lib/widgets/row-actions.ts` — `oneClickRowActionsForType(type, ontology)`: returns actions whose ONLY required param is a ref to the row's object type. For agent_blocker → exactly `dismiss_blocker` (blocker_id req ref + reason optional); `resolve_blocker_with_*` EXCLUDED (2nd required param), `flag_blocker` excluded (ref targets Member). Pure + unit-tested (3 tests).
- `lib/widgets/row-action.server.ts` — `"use server"` `invokeRowAction(action, objectId)`: mirrors confirm/route.ts (buildChatRuntime → anon/non-steward reject → createInProcessDispatcher → runApplyActionTool bypassConfirmation) but TIGHTER: validates `action` is a one-click row action (ontology-derived) + constrains params to `{[refParam]: objectId}`, so the surface can't invoke arbitrary actions/params. The steward's click IS the always_confirm human confirmation. revalidatePath("/org") on success.
- data_table opt-in `row_actions`; `ResolvedWidget.rowActions` populated at resolve time (runDescriptors has the ontology); `DataTableWidget` hides `id` from display, uses it as the action target, renders `<form action>` buttons (semantic tokens). Generic data_tables unaffected. Default veto-queue: `id` prepended to columns + `row_actions: true`.

**CRITICAL BUG FOUND + FIXED (da2807d9e) — this is the headline.** Clicking Dismiss in Chrome (the FIRST time a function-backed action ran through a real Next runtime — the chat was always too flaky, so the confirm route was never actually exercised in-browser) threw `FunctionBackedActionError: Cannot find module as expression is too dynamic` at function-backed.ts. ROOT CAUSE: `loadFunctionBackedAction` did `import(runtimeFileUrl)` — Turbopack CANNOT bundle a fully dynamic import. So EVERY function-backed action (dismiss_blocker, check_in, check_out, resolve_*, change_tier, flag_blocker, mark_notification_read, promote_to_steward) was NON-FUNCTIONAL in the deployed app — invisible to the green vitest suite (node resolves dynamic paths) and to the deterministic round-trip tests (cycle 9 proved LOGIC, never the Next/Turbopack bundling). FIX: static import registry (`lib/actions/function-registry.ts`, 10 descriptors) that Turbopack bundles; loader consults it first for the canonical functions dir. **Subtlety that bit once:** the registry gate must compute the canonical dir via `process.cwd()` (matching chat-runtime) — the implementer first used `__dirname`, which Turbopack rewrites to a chunk path → gate never matched → still fell through to the dynamic import (verified by a 2nd Chrome 500). Switching to process.cwd() fixed it. Drift-guard test added.

**VERIFY (Chrome = ground truth, the full loop):** /org veto-queue shows a "Dismiss Blocker" button per open blocker (id hidden; bed-inventory below has NO Actions column — opt-in works). Clicked Dismiss on "Charge EUR 40 no-show fee to booking BK-0007?" → server-action POST /org **200** → DB: open blockers **3→2**, that row `status=dismissed` → re-rendered queue shows **2** rows, the dismissed one GONE. tsc 0; action suites (invoke/composition/function-backed/function-registry/resolve-blocker) 32/32; widget+dashboard 59/59 (earlier).

**Coherence:** governed/derived (no per-type hardcoding), fail-closed (steward gate + ontology-validated action + single-ref param + enforceActionPermission still runs — bypassConfirmation only skips the prompt, not the perm check), clean. The registry is a legit static-bundling necessity, not a duplicate abstraction.

**Next frontier:** (a) richer affordances — resolve_blocker_with_pathway needs a pathway picker (the confirm_binary/pathways blockers want Confirm + choose-path, not just Dismiss); this is the other half of the veto surface. (b) Now that function-backed actions PROVABLY work in-app, the chat confirm flow (check_in/out etc.) can finally be exercised in-browser — worth a real end-to-end chat governance demo. (c) fold widgetLabel→title (VN-17 LOW). Via Negativa: audit the new write surface HARD — can a non-steward reach invokeRowAction? can `action` be coerced to a non-row-action or `objectId` to a foreign type? is revalidatePath the only side effect? confirm the registry gate holds under a production-style build, and that bypassConfirmation here cannot escalate (perm check still runs).

### 2026-05-26 (midday) — Via Negativa (cycle 19, audit of the row-action write surface) — HIGH found + FIXED
Hard audit of the new client-reachable write surface (`invokeRowAction`), as VP-18 requested. Verified in Chrome (tab 1025355378, steward): veto-queue renders Dismiss per open blocker; bed inventory has no Actions column (opt-in holds).

**HIGH — FIXED THIS CYCLE (a6311a47e): confirmation-bypass on privileged actions via an over-broad row-action endpoint.**
`invokeRowAction(action, objectId)` (row-action.server.ts) is steward-gated and sets `bypassConfirmation=true`, but accepted ANY action that *structurally* qualifies as one-click — the rule being "exactly one required parameter, an inline ref." I checked the ontology: that rule ALSO admits:
- `promote_to_steward` — `member` (ref<Member>, required) → QUALIFIES. `invokeRowAction("promote_to_steward", anyMemberId)` would mint a steward, `always_confirm` BYPASSED.
- `check_in` / `check_out` — `booking` (ref<Booking>, required) → QUALIFY → confirmation-bypassed on arbitrary bookings.
- (`change_tier` excluded — 2 required; `resolve_*` excluded — 2 required; `flag_blocker` excluded — 4 required.)
The rendered UI only shows Dismiss for agent_blocker, BUT (a) the server action is a POST endpoint reachable independent of the button, and (b) `oneClickRowActionsForType("member")` would have rendered **promote_to_steward buttons on every member row** if a steward composed a `member` data_table with `row_actions:true`. So the structural rule was necessary but NOT sufficient — it silently defeated the `always_confirm` governance contract (the system's core primitive) for privileged actions. Note: the dispatcher's `enforceActionPermission` running does NOT mitigate this — `promote_to_steward` is permissioned `[steward]`, so a steward passes the perm check; the ONLY thing standing between a steward session and a silent promotion was the (absent) confirmation. Next.js server-action CSRF/Origin guards limit EXTERNAL reach, but an in-origin script (XSS/extension) or the composed-member-widget path makes it real.
**FIX:** `ROW_ACTION_SAFELIST = {dismiss_blocker}` (row-actions.ts), enforced in BOTH `oneClickRowActionsForType` (render) and `resolveRowActionRefParam` (server invocation gate, checked FIRST). Only vetted actions can surface as confirmation-bypassing one-click affordances. Regression test added (promote_to_steward NOT exposed for member; safelist == [dismiss_blocker]). tsc 0; row-actions 4/4; widget suites 48/48. Chrome: Dismiss still renders + works.

**MED/LOW: none new beyond the above.** Steward identity gate is server-side + fail-closed (anon→unauthorized, non-steward→forbidden), mirroring the confirm route — sound. Params correctly constrained to `{[refParam]: objectId}` (no arbitrary-param smuggling). `revalidatePath("/org")` is the only side effect.

**ONE concrete improvement suggestion (the PRINCIPLED fix — for Via Positiva):** replace the code-level safelist with an ONTOLOGY opt-in. Add an optional field to action_types (e.g. `row_action: true`, or `surfaces: [row_action]`) in the YAML + schema; have `oneClickRowActionsForType` and the server gate require it. Then `dismiss_blocker` declares itself a row action and the safe set is governed by the ontology (the adaptive-architecture way), not a hand-maintained Set in code. This also lets future safe actions opt in without touching TS. The current safelist is a correct INTERIM control; the ontology opt-in is the coherent end state.

**Ontological-coherence verdict: GOOD, with the caveat above.** The row-action mechanism is otherwise well-governed (derived from action signatures, dispatched through the real pipeline, fail-closed identity). The structural-derivation-without-an-opt-in was the coherence gap — a purely structural rule made a SECURITY decision (what's safe to one-click-bypass-confirm) it wasn't fit to make. The interim safelist plugs it; the ontology opt-in restores full coherence.

**Quality-trend: IMPROVING — and the dialectic is proving its worth.** Cycle 18 shipped a real feature AND surfaced a critical infra bug via ground truth; this cycle caught a real HIGH governance hole in that same feature's write surface before it could bite. Two consecutive cycles where in-browser/adversarial verification found what green tests missed. Next Positiva: the ontology opt-in (replaces the safelist) — and only THEN consider widening row actions (e.g. a resolve/confirm affordance with a pathway picker), since widening is exactly what the safelist now (correctly) blocks.

### 2026-05-26 (midday) — Via Positiva (cycle 20, build) — row-action safety is an ONTOLOGY OPT-IN, not a code safelist (efc1d5080)
Honored VN cycle-19's #1 fix-request (the principled replacement for the interim safelist).
- **`ActionType` schema (lib/ontology/schema.ts)** gains optional `row_action: z.boolean().optional()` (the schema is `.strict()`, so the field had to be declared before any YAML could carry it).
- **`dismiss-blocker.yaml` opts in** (`row_action: true`) — in BOTH the active `ontology/` and the `seed/small-community/` template (kept in sync). Comment notes it's safe because dropping an escalation is low-stakes + reversible (agent can re-flag).
- **`ROW_ACTION_SAFELIST` (the hand-maintained Set) DELETED** (clean-break). Replaced with `isRowActionEnabled(def) => def.row_action === true`, enforced in BOTH `oneClickRowActionsForType` (render) and `resolveRowActionRefParam` (server invocation gate). The safe set now lives in the ONTOLOGY — new safe actions opt in via YAML, no TS change. promote_to_steward / check_in / check_out lack the flag → still excluded (the VN-19 hole stays closed, now governed).
- **Tests updated** (row-actions.test.ts, 5/5): agent_blocker → dismiss_blocker (opt-in present); promote_to_steward NOT exposed for member (no opt-in); `isRowActionEnabled` true for dismiss_blocker, false for check_in/check_out.
- **VERIFY (Chrome = ground truth, steward):** /org veto-queue renders "Dismiss Blocker" per open blocker via the opt-in path; clicked Dismiss on "Overbook bed D3-A2 for tonight walk-in?" → server-action POST 200 → DB open blockers **2→1**, that row `status=dismissed`, re-rendered queue shows **1** row (the dismissed one gone). tsc 0; row-actions 5/5; widget suites green. (The 7 lib/ontology test failures are PRE-EXISTING baseline — `seed.test` is an object-type-list expectation drift `['AgentBlocker','Event',…]` vs `['Event','MeetingMinute',…]`, unrelated to row_action; confirmed by reading the assertion error.)

**Coherence:** the row-action mechanism is now FULLY governed — affordance derivation (structural single-ref rule) AND the safety gate (ontology opt-in) both flow from the ontology, no code-level per-action literal. This is the adaptive-architecture end state VN-19 asked for. The ontology is now the single source of truth for WHICH actions are one-click-safe.

**Next frontier:** (a) widen row actions safely — now that opt-in exists, a `resolve` affordance for `resolve_blocker_with_pathway` needs a pathway PICKER (it has a 2nd required param, so it's NOT one-click; needs a small form/confirm UI, not a bare button) — this is the other half of the veto surface (Confirm-a-path vs Dismiss). (b) the now-unblocked in-browser chat governance demo (check_in via chat → confirm → handler, finally exercisable post-cycle-18 registry fix). (c) widgetLabel→title fold (VN-17 LOW). Via Negativa: confirm the opt-in gate is enforced on BOTH paths (render + server) — try to invoke a non-opted-in action (e.g. check_in) through invokeRowAction and confirm it returns not_a_row_action; confirm no action without row_action:true renders a button even if structurally single-ref.

### 2026-05-26 (midday) — Via Negativa (cycle 21, audit of the ontology opt-in refactor)
Independent verification (reproduced, not trusted): Chrome (steward) /org shows the 1 remaining open blocker ("Guest 2 days past expected departure") with its "Dismiss Blocker" button → the opt-in RENDER path works (dismiss_blocker has row_action:true). Last cycle's invoke (2→1) stands. Re-ran lib/widgets/row-actions.test.ts 5/5; tsc 0.

**SECURITY — the opt-in gate is enforced on BOTH paths (VP-20's ask), verified by code-read:**
- SERVER gate (`resolveRowActionRefParam`, row-action.server.ts) order is correct + fail-closed: `def` lookup → `if (!def) return null` → `if (!isRowActionEnabled(def)) return null` (opt-in FIRST) → single-required-inline-ref check. So `invokeRowAction("check_in", …)` → not opted-in → null → `not_a_row_action`. The boundary holds independent of the rendered UI. promote_to_steward/check_in/check_out all lack `row_action:true` → excluded.
- `bypassConfirmation` does NOT skip permission (verified tool-gating.ts:106-160): it only skips the policy/confirmation gate (106-130); the dispatcher is ALWAYS called (146) and `ActionPermissionError` → `permission_denied` (155). So enforceActionPermission still runs under bypass. The row-action path's security claim is sound.
- Steward identity gate (anon→unauthorized, non-steward→forbidden) intact.

**HIGH/MED: none.** The refactor is correct and the safe set is now governed by the ontology — exactly the end state requested.

**LOW (coherence — latent drift risk on a SECURITY boundary): the structural one-click rule is implemented TWICE.** `qualifyingRefParam` (row-actions.ts, used by the tested render helper) and the inline logic in `resolveRowActionRefParam` (row-action.server.ts, the UNTESTED invocation gate) BOTH encode "exactly one required param that is an inline ref." Only `isRowActionEnabled` is shared (cycle 20's win). The two structural impls agree TODAY, but a future edit to one could silently diverge — and the server one is the real boundary yet has no direct test (it's unexported + depends on buildChatRuntime). Echoes the earlier two-`ontologyToGraph` duplication the loop is wary of.

**LOW (test-debt, PRE-EXISTING, out of loop scope but flagging the trend): 7 lib/ontology tests fail as baseline** — `seed.test` (object-type list drift: actual `['AgentBlocker','Event',…]` vs expected `['Event','MeetingMinute',…]`), `seed-delete-member` ("delete_member must exist… expected undefined"), 4× `links.attended` ("Cannot read properties of undefined") + ctx-runtime wiring. NONE mention row_action — cycle 20 added ZERO failures (verified by reading every error). But these stale failures erode the regression signal: a real ontology regression could hide among them. Someone should refresh seed.test's expectations to the evolved seed (or delete the obsolete delete_member assertion).

**ONE concrete improvement suggestion (the fix for the LOW above):** extract ONE shared `requiredRefParam(def): string | null` (the "single required inline-ref param" rule) into row-actions.ts; have BOTH `qualifyingRefParam` (which then adds the target-match) and `resolveRowActionRefParam` call it. Then add a deterministic test of the server gate's qualification (assert `resolveRowActionRefParam`/an exported equivalent returns null for check_in/promote_to_steward and "blocker_id" for dismiss_blocker) so the invocation boundary — not just the render helper — is covered. One rule, one impl, one test.

**Ontological-coherence verdict: STRONG.** Cycle 20 moved the safety decision from a code Set into the ontology (`row_action: true`) — the governed, adaptive-architecture end state. Derivation (structural) + safety (opt-in) both flow from the ontology now. The only nit is the duplicated structural impl above (LOW).

**Quality-trend: STEADY-EXCELLENT.** Three cycles of substantive frontier work (actions → infra fix → security hole → governed opt-in) with the dialectic catching real issues; this cycle found no new defect, only a latent duplication to consolidate + pre-existing test-debt. Next Positiva: either the shared-helper consolidation (small, closes the LOW + adds boundary coverage) OR the next frontier — a `resolve_blocker_with_pathway` affordance (needs a pathway PICKER, since it's a 2nd-required-param action, NOT one-click → a small governed form, the Confirm half of the veto surface). Recommend the consolidation first (cheap, hardens the boundary), then the picker.

### 2026-05-26 (midday) — Via Positiva (cycle 22, build) — consolidated the row-action rule (one impl, render+server) + boundary test (ff1798b87)
Honored VN cycle-21's #1 fix-request (cheap hardening before the next frontier).
- **Extracted the shared structural rule** into row-actions.ts: `requiredRefParam(def)` (single required inline-ref param, type-agnostic) + `rowActionRefParamFor(def)` (= opt-in AND structural — the server's EXACT check). `qualifyingRefParam` (render) now layers the target-type match on `requiredRefParam`; `resolveRowActionRefParam` (row-action.server.ts) is now a thin `rowActionRefParamFor(def)` call. The "single required inline-ref" logic — previously duplicated in render (tested) and server (untested) — lives in ONE place. Clean-break: the inline server re-derivation + the stale `isRowActionEnabled` import are gone.
- **Boundary test added** (row-actions.test.ts, now 8 tests): `rowActionRefParamFor` directly — dismiss_blocker → "blocker_id"; check_in/check_out/promote_to_steward → null (single-ref but NOT opted-in); resolve_blocker_with_pathway → null (2nd required param). The SECURITY boundary (server gate), not just the render helper, is now covered — a future drift in the rule fails a test.
- **VERIFY:** tsc 0; lib/widgets 52/52 (row-actions 8/8). Chrome (steward): /org veto-queue still renders "Dismiss Blocker" on the 1 open blocker via the refactored shared path — behavior-preserving (no re-click needed; render proves the path; invoke was proven cycles 18+20 and the gate logic is now unit-covered).

**Coherence:** the row-action mechanism is now maximally DRY + governed: opt-in (`isRowActionEnabled`) + structural (`requiredRefParam`) + the composed gate (`rowActionRefParamFor`) are each defined once and shared across render + server. The VN-21 drift risk is closed.

**Next frontier (the Confirm half of the veto surface):** a `resolve_blocker_with_pathway` affordance. Unlike Dismiss it is NOT one-click — it has a 2nd required param (`pathway_id`), and the blocker row carries `pathways` (a JSON array of {id,label,...}). So this needs a small GOVERNED picker: render the blocker's pathway options as buttons, each invoking resolve_blocker_with_pathway with {blocker_id, pathway_id}. Design notes for next Positiva: (a) it's a NEW affordance shape (multi-param, choice-driven) — likely a new opt-in (e.g. `row_action` won't cover it; maybe `row_resolver: true` or a richer surfaces field) so it doesn't get mis-rendered as a one-click button; (b) the pathways come from the row data (resolution_mode=pathways), so the widget needs the `pathways` column fetched; (c) confirm_binary blockers (confirm_action) are a third shape. Pressure-test the affordance-shape vocabulary before building. Via Negativa: confirm the consolidation is behavior-identical (no action newly admitted/excluded vs cycle 21) + the new boundary tests genuinely fail if the opt-in or structural check is removed.

### 2026-05-26 (afternoon) — Via Negativa (cycle 23, audit of the row-action consolidation + post-merge state)
Context: between cycles the USER merged feat→main (fast-forward, local, not pushed — main now == feat tip 72ce9469e) and asked about the left nav (answered: hand-coded ITEMS in left-nav.tsx, not codegen). Loop work continues on feat.

Independent verification of cycle 22 (the shared-rule consolidation): tsc 0; lib/widgets 52/52. Chrome (steward) /org renders 3 "Dismiss Blocker" buttons (user re-opened the demo blockers) via the refactored render path — behavior-preserving confirmed.
**MUTATION TEST (VN-21's explicit ask — does the new boundary test actually bite?):** removed the `isRowActionEnabled` opt-in check from `rowActionRefParamFor`, re-ran → `rowActionRefParamFor(check_in)` returned `'booking'` and the test FAILED with "expected 'booking' to be null". Reverted; 8/8 green. So the security boundary is GENUINELY test-covered — removing the gate is caught. VN-21's concern is fully closed.

**HIGH/MED: none.** The consolidation is behavior-identical (requiredRefParam = the old structural core; rowActionRefParamFor = opt-in + structural = the old server gate; qualifyingRefParam = requiredRefParam + target match = the old render rule). One rule, three composed exports, each defined once. `row_action` is read ONLY by isRowActionEnabled (grep-confirmed) — no stray consumer.

**LOW (hygiene/artifact-drift — from cycle 20, not 22, but surfaced by the merge): the committed `lib/inngest/declarative-actions.generated.ts` is STALE vs the live ontology.** Its embedded ontology JSON snapshot predates cycle-20's `row_action` addition; the working tree regenerates it at boot (the known "generated files regenerated from live" gotcha) → the file is PERPETUALLY dirty (`M` every cycle). Functionally harmless for row_action (inngest doesn't consume it), BUT: (a) a fresh `next build` without boot-regen would embed a stale ontology; (b) a permanently-dirty generated file masks real drift and clutters `git status` (it was nearly swept into commits before). This is the kind of thing that bites later.

**LOW (coherence — from the user's nav question): the left nav is hand-coded** (`components/shell/left-nav.tsx` ITEMS literal), not ontology-derived. Defensible for the invariant shell routes (Home/Org/Graph/Ontology), but the OBJECT-TYPE browse entries could be generated from `ontology.object_types` (precedent: the `app/(generated)/[type]` route already renders any type generically). Mixing a fixed list with no derived section is a small opinionated-hand-coding gap vs the adaptive vision. Pre-existing; not urgent.

**ONE concrete improvement suggestion:** resolve the generated-file policy. Either (a) `.gitignore` the boot-regenerated artifacts (`lib/**/*.generated.ts` that are rebuilt at boot) so they stop showing as dirty + can't be accidentally committed stale, OR (b) make the regen step part of the commit flow so a committed snapshot always matches the source ontology. Right now it's the worst of both — committed AND regenerated → always dirty, sometimes stale. Pick one. (This is also a guardrail against the "ai-grants/uploads swept into a commit" class of mistake the loop has hit.)

**Ontological-coherence verdict: STRONG for the row-action mechanism** — it's now maximally DRY, ontology-governed (opt-in + structural both derived), and the security boundary is mutation-test-proven. The two LOWs are at the EDGES (a generated-artifact hygiene policy; the shell nav) — neither is in the row-action/view-gen core. No new fragmentation or duplicate abstraction (the consolidation REMOVED a duplication).

**Quality-trend: EXCELLENT / stabilizing.** The actions arc (cycles 18–22) is complete and hardened: feature shipped + verified in-browser, a critical infra bug fixed, two security holes closed, the safelist promoted to an ontology opt-in, the rule consolidated + boundary-tested. This cycle found no core defect — only edge hygiene. Healthy sign the arc has converged. Next Positiva: (a) the generated-file policy fix (cheap, closes the LOW + a real footgun) OR (b) the next frontier — the `resolve_blocker_with_pathway` PICKER (the Confirm half of the veto surface; needs a new multi-param affordance shape — pressure-test the shape vocabulary first, don't shoehorn it into the one-click path). Recommend (a) first (tiny), then (b).
