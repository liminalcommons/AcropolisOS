
### 2026-05-26 ~ — Via Negativa (cycle 10, audit of the governance round-trip test + check_out)
Per cycle-9-Negativa's own standard, I did NOT drive the flaky chat. I audited the deterministic deliverables directly.

**Round-trip test (`check-in-confirm-roundtrip.test.ts`) — VERIFIED GENUINE, NOT a tautology.** Read all 209 lines. It wires the REAL machinery: `loadOntology(ONTOLOGY_ROOT)`, `buildObjectPermissionsMap`, `createCtx` (the permission wrapper), `createInProcessDispatcher` pointed at the REAL `functions/` dir, `runApplyActionTool` + `resolveActionPolicy`. Only the *store* is `createInMemoryStore` — the legitimate DB double; the permission wrapper (`wrapObjectAccess`) is store-agnostic (Step 1), so the gate behaves identically to pg-store. **Step A** (no bypass): asserts `ok:false` + `confirmation_required` envelope (action/params/`reason:"always_confirm"`/`required_permissions:[steward,manager]`) + `result.result` undefined AND re-reads Booking (still `confirmed`) + Guest (still `booked`) → proves the handler genuinely did NOT run. **Step B** (`bypassConfirmation:true`, exactly what `confirm/route.ts` sets server-side): asserts `ok:true`, no confirmation envelope, the handler's checked-in envelope, Booking/Guest read back `checked_in`, and an `ok` audit row linked by `audit_id`. This is a real agent→confirm→handler→mutation proof. Strong.

**check_out handler — VERIFIED CORRECT MIRROR (read functions/check-out.ts).** Not a copy-paste error: Booking→`completed` (correct, NOT `checked_in`), Guest.current_status→`checked_out`. Same `findById` existence guard, same `update_failed` guard, same guest-coherence (graceful if guest missing/unreadable), same fail-closed permission inheritance (dispatcher gates [steward,manager] before handler; ctx.objects re-enforces). agent_policy unchanged.

**Re-ran independently: 8/8 pass** (`check-in-confirm-roundtrip` + `check-out` test). tsc clean (prior).

**Findings:** HIGH/MED security or correctness: NONE this cycle. The round-trip test is a faithful, store-double-only integration proof; the check_out mirror is correct and double-fail-closed.
- **One genuine finding (PRE-EXISTING, not this cycle's regression):** `dismiss_blocker` declares `function: dismiss-blocker` in `ontology/action-types/dismiss-blocker.yaml` but `functions/dismiss-blocker.ts` DOES NOT EXIST → the action is un-executable (fails at function resolution; this is why `resolve-blocker-with-pathway.test.ts` fails). Same class as check_in/check_out before cycle 8. It is a real platform hole — a declared platform action that cannot run — but it predates this loop.

**Ontological coherence verdict: GOOD.** No new abstraction. The round-trip test reuses the same harness as `log-incident-auto-apply.test.ts`; check_out reuses the function-backed `defineAction` pattern. Verification has fully pivoted off the flaky chat onto deterministic integration tests — the right call.

**Quality trend: EXCELLENT.** The governance contract is now LOCKED deterministically on both arms (auto_apply commits; always_confirm gates → confirm applies), with the round-trip test as the durable standard. The remaining frontier is genuinely NEW, not polish: (1) **fix `functions/dismiss-blocker.ts`** (un-executable platform action — highest-value next build, closes the last function-backed hole) + `functions/claim-shift.ts`; (2) the **veto-queue VIEW** — always_confirm actions currently return `confirmation_required` INLINE one-at-a-time in chat; the user's testbed vision ("28 auto-applied, M awaiting you") needs a PERSISTENT queue surface the steward can watch — this is the real unbuilt half of the AI-governance testbed; (3) Step 3 /day-as-generated. Recommend Positiva take (1) then (2).

### 2026-05-26 ~ — Via Positiva (cycle 11, build) — dismiss_blocker handler: LAST un-executable function-backed action closed (75742aad0)
Honored cycle-10-Negativa's #1 fix-request before anything new.
- **`functions/dismiss-blocker.ts`** — the missing handler that made `dismiss_blocker` un-executable (failed at function resolution) and broke `resolve-blocker-with-pathway.test.ts`. A faithful mirror of `resolve-blocker-with-pathway.ts`: `findById` guard → `not_found`; `status !== "open"` → `not_open` (status echoed); `ctx.objects.AgentBlocker.update(status:"dismissed")` (ONLY status — AgentBlocker has no `dismissed_at` column, enum is [open,resolved,dismissed,expired]); self-notify (kind `agent_unblocked`, carries `reason` if supplied) so the agent principal can re-prompt. Returns `{ ok, blocker_id }`.
- **Permission is upstream, not in the handler** (correct, matches the mirror): `member_self` ownership is enforced by `enforceActionPermission` BEFORE the handler — cross-member dismiss + non-existent-blocker both `reject(/cannot invoke action/)`; steward bypasses ownership. The handler is intentionally role-agnostic. agent_policy unchanged (always_confirm).
- **VERIFY: 9/9 pass** (re-ran independently: 5 existing resolve + 4 new dismiss — own/cross-deny/not-undoable/steward). **tsc clean** (no dismiss-blocker errors). `git status` confirms ONLY `functions/dismiss-blocker.ts` added — no stray files.
- **No Chrome step: this is a backend governance handler, not a route/component.** Per the cycle-9/10 governance-verification standard (chat UI chronically flaky), the deterministic test IS ground truth here. No browser claim made.

**Governance state now COMPLETE on the action layer:** every function-backed action has a handler — check_in ✓, check_out ✓, dismiss_blocker ✓ (this cycle). `claim_shift` is the only remaining one to double-check (targets Shift — confirm assignee field + member-less-steward semantics; may already work via the dispatcher). auto_apply (log_incident) + always_confirm (check_in/out, dismiss_blocker) both proven.

**Next frontier (the real unbuilt half — recommend Positiva take this next):** the **veto-queue VIEW**. Right now always_confirm actions surface `confirmation_required` INLINE, one-at-a-time, in chat. The user's testbed vision ("AI applies a batch → 28 auto-applied, M awaiting you") needs a PERSISTENT queue surface the steward can watch — a place where pending always_confirm actions accumulate with Confirm/Reject. Also: AgentBlocker rows (open status) are exactly this kind of "awaiting human" queue and already have read perms — a `/me`-style or `/org` veto/blocker queue widget (governed catalog: a new "queue" or reuse roster/data_table over AgentBlocker filtered status=open) would surface BOTH agent-blockers AND pending confirmations. THIS is the keystone of the AI-governance testbed and is genuinely unbuilt. Via Negativa: audit dismiss_blocker (mirror correctness + that the self-notify can't leak cross-member) — it's deterministic, no chat needed.

### 2026-05-26 ~ — Via Negativa (cycle 12, audit of dismiss_blocker handler)
Independent verification (NOT trusting the log): re-ran `functions/resolve-blocker-with-pathway.test.ts` → **9/9 pass** (5 resolve + 4 dismiss). `tsc --noEmit` → **0 `error TS`** (genuinely clean, confirmed by `grep -c`). No Chrome step: dismiss_blocker is a backend handler with no route/component, the veto-queue view isn't built yet, so there is no UI surface to screenshot — deterministic tests are ground truth here (consistent with the cycle-9/10 standard). Honest: no browser claim.

**HIGH:** none.

**MED — WRONG NOTIFICATION RECIPIENT in the steward-override path (real, untested, defeats documented intent).**
`functions/dismiss-blocker.ts:` notifies `recipient_member_id: ctx.actor.userId`. But the action's own doc says "agent principal is notified so it can re-prompt," and the principal is the BLOCKED member (`row.blocked_actor_id`), not the dismisser. Proof chain:
- `functions/flag-blocker.ts:70` writes the blocker notification to `recipient_member_id: params.blocked_actor_id` (the principal) — the correct counterpart.
- `app/inbox/page.tsx:40` filters `listForRecipient(actor, actor.userId)` — a notification is ONLY ever visible to its `recipient_member_id`.
- Therefore when a STEWARD dismisses member A's blocker (the explicit "steward can dismiss any member's blocker" test, line 252), the "Dismissed" notification lands in the STEWARD's inbox; member A — and the agent watching member A's flow — never learn the blocker was cleared, so the agent stalls or re-prompts blindly. Self-notify is only coincidentally correct when `actor === blocked_actor_id` (self-dismiss).
- Untested: the 4 dismiss tests assert `ok`/`status` only, never the notification recipient, so the bug is invisible to the suite.
- **PATTERN, not isolated:** identical line in `resolve-blocker-with-pathway.ts:39`, `resolve-blocker-with-input.ts:56`, `resolve-blocker-with-custom.ts:49` — all 4 blocker-clearing handlers notify `ctx.actor.userId`. Cycle 11 faithfully MIRRORED a latent pre-existing defect. The 3 resolve handlers predate this loop, but the bug is the same.

**FIX REQUEST for Via Positiva (TDD, consistent across all 4 — do this BEFORE building the veto-queue view, or the queue will let a steward silently fail to notify members):**
1. RED: add a test to `functions/resolve-blocker-with-pathway.test.ts` — steward dismisses member A's OPEN blocker → assert `notifications` contains a row with `recipient_member_id === memberA.userId` (NOT the steward's). It will fail today.
2. GREEN: in all four handlers change `recipient_member_id: ctx.actor.userId` → `recipient_member_id: row.blocked_actor_id`. Correct for self-clear (actor===blocked member) AND steward-override (notifies the principal). `row.blocked_actor_id` is already loaded via `findById` in each handler.
3. Re-run; 9/9 + the new test green; tsc clean.

**LOW (observation, not a defect):** the four blocker-clearing handlers share a near-identical self-notify block. After the recipient fix, consider a tiny shared `notifyPrincipal(ctx, row, kind, body)` helper to keep the recipient correct in ONE place (YAGNI until the 4th was added — now it's the 4th, so it's borderline justified). Defer unless it recurs.

**Ontological-coherence verdict: GOOD with a caveat.** dismiss_blocker correctly reuses the function-backed `defineAction` pattern + upstream `member_self` enforcement (cross-member deny + steward bypass verified by tests) + `ctx.objects`. No new abstraction, no free-form code, fits the adaptive-architecture vision. The caveat is the propagated recipient bug above — coherence of the PATTERN was preserved at the cost of copying its flaw.

**Quality trend: SOLID but watch the mirror reflex.** Cycle 11 closed a real hole (last un-executable function-backed action) cleanly and fast. The risk this cycle surfaced: "faithful mirror" propagated a latent defect verbatim. Net still positive — the action layer is complete — but the recipient fix should land before the veto-queue view is built on top of these handlers.
