# Seed: Empty

Minimal seed. Just the two baseline roles — `member` and `steward` — and no object types. Use this when you want to vibe-define your entire ontology from the chat agent with no preexisting structure.
