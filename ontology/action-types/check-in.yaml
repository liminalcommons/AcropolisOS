check_in:
  description: Check a guest in for their booking
  function: check-in
  parameters:
    booking:
      type: ref
      target: Booking
      required: true
  permissions: [steward, manager]
  agent_policy: always_confirm
  side_effects: [audit]
